package main

func main() {
	// points, err := client.NewBatchPoints(client.BatchPointsConfig{
	// 	Database: "test",
	// })
	// if err != nil {
	// 	log.Fatalln(err)
	// }
	//
	// p, err := client.NewPoint("example", map[string]string{"tag1": "foo"}, map[string]interface{}{"field1": 1}, time.Now().UTC())
	// if err != nil {
	// 	log.Fatalln(err)
	// }
	// points.AddPoint(p)
	//
	// cc := client.NewUnixSocket("/tmp/telegraf.sock")
	// // log.Println(cc.Ping(0 * time.Second))
	// log.Println(cc.Write(points))
}
